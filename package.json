{
  "name": "tracebeam",
  "private": true,
  "version": "0.6.0",
  "packageManager": "pnpm@11.17.0",
  "type": "module",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "web": "vite",
    "web:build": "tsc && vite build",
    "test:security": "node --test tests/*.test.ts",
    "version:set": "node scripts/set-version.mjs"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.8.0",
    "@tauri-apps/plugin-dialog": "^2.4.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.8.0",
    "typescript": "^5.9.2",
    "vite": "^7.0.6"
  }
}
