{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "windows-controls",
  "description": "Custom Windows window controls",
  "windows": ["main"],
  "platforms": ["windows"],
  "permissions": [
    "core:window:allow-minimize",
    "core:window:allow-toggle-maximize",
    "core:window:allow-close"
  ]
}
