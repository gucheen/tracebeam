# Tracebeam v0.2.0

相较于 v0.1.0，本次更新重点提升了大文件浏览性能、日志操作效率和桌面端发布体验。

## 主要更新

- 新增虚拟滚动，只渲染当前视口附近的日志行，大量日志下滚动更流畅。
- 新增查询结果缓存，重复分页和滚动时无需反复扫描整份日志。
- 新增日志多选：支持单条选择、Shift 连选、选择当前可见项以及一键清空。
- 新增原始日志批量复制，可通过工具栏或 `Ctrl/Cmd + C` 复制所选 JSONL 原文。
- 新增签名在线更新：应用可自动或手动检查 GitHub Releases，下载更新后自动安装并重启。
- 优化“跟随最新日志”行为：手动向上滚动后会自动停止跟随，避免视图被新日志拉回底部。

## 修复与优化

- 修复多选日志时可能导致界面白屏的问题。
- 修复多选状态与可见行不同步的问题，并优化 Shift 连选逻辑。
- 修复选择工具栏在窗口较窄时溢出的问题。
- Windows 发布版不再额外显示控制台窗口。
- 更新各平台应用图标资源。
- 重构前端结构，拆分后端调用、类型与格式化工具，降低后续维护成本。

## 发布与开发流程

- 包管理器由 npm 切换为 pnpm。
- GitHub Actions 现在构建 macOS Apple Silicon、macOS Intel 和 Windows 安装包。
- 发布流程会生成签名更新包及 `latest.json`，供应用内更新使用。
- 安装包目标精简为 macOS DMG 和 Windows NSIS。
- 新增统一版本命令 `pnpm version:set <version>`，一次同步三个版本字段。

**完整变更记录：** [latest...v0.2.0](https://github.com/gucheen/tracebeam/compare/latest...v0.2.0)
